# bili-sign

本服务基于 `better-auth` 与 插件 `better-auth-bili-basic` ，公开提供一个 OAuth2.1 授权服务，供开发者使用。

用户仅需在此处验证过一次B站账号签名，即可通过该服务的 OAuth2.1 向其它应用认证对某B站账号的所有权。

## 须知

该方法**无法**也**不会**获取用户的隐私信息(所有需要access_token才能获取到的信息)，只会暴露用户的公开信息。

验证过程仅需在用户所拥有的B站账号上临时修改一下签名，添加指定验证码即可。
